apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-world-from-db.fullname" . }}-test-connection"
  labels:
    {{- include "hello-world-from-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-world-from-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

# TODO: implement more tests